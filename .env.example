# Hotelly V2 — ambiente local (exemplo)
#
# Uso recomendado:
#   cp .env.example .env.local
#   # então, exporte a variável ou passe inline:
#   DATABASE_URL="${DATABASE_URL}" make migrate
#
# Observação: Make/uv NÃO carregam automaticamente .env.local.
# Você deve exportar as variáveis no shell, ou passar inline no comando.

# Postgres (docker compose)
POSTGRES_DB=hotelly
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# String de conexão usada por Alembic/app
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hotelly

# App local (quando rodar uvicorn)
APP_HOST=127.0.0.1
APP_PORT=8000
LOG_LEVEL=INFO

# Tasks backend
TASKS_BACKEND=http  # inline | http | cloud_tasks
WORKER_BASE_URL=http://worker:8000
INTERNAL_TASK_SECRET=change-me-in-production

# Evolution API (WhatsApp Business - self-hosted)
# EVOLUTION_BASE_URL=http://localhost:8080
# EVOLUTION_INSTANCE=your-instance
# EVOLUTION_API_KEY=your-api-key

# Meta Cloud API (WhatsApp Business - official)
# META_APP_SECRET=your-app-secret
# META_VERIFY_TOKEN=your-verify-token
# META_PHONE_NUMBER_ID=your-phone-number-id
# META_ACCESS_TOKEN=your-access-token
# META_DEFAULT_PROPERTY_ID=fallback-property-id
# META_GRAPH_API_VERSION=v18.0
